[Main Page](README.md)

# Unofficailly available commands

## /bin
```
 > echo $(ls /bin)
acs_cli acsd addgroup adduser adsl adslctl ash bash bcmmserver bpm bpmctl brctl busybox cat celld chat chattr chgrp chmod chown consoled cp cpuload cttyhack date dd ddnsd delgroup deluser df dhcp6c dhcp6ctl dhcp6relay dhcp6s dhcpc dhcpd dmesg dnsdomainname dnsproxy dnsspoof dry dsldiagd dumpmem eapd ebtables echo egrep epi_ttcp ethctl ethswctl false fap fapctl fc fcctl fgrep getopt grep hostname hotplug httpd ip ip6tables ipaddr iplink ippd iproute iptables iq iqctl kill link_updown lld2d ln login ls lsattr lsusb mailsend mcpd mkdir mknod mktemp more mount mountpoint mroute mv nas nas4not netstat nice nmbd ntfs-3g nvram nvramUpdate openssl pidof ping ping6 pppd ps pwd pwr pwrctl radvd rawSocketTest ripd rm sed setmem sh sleep smbd smd snmpd sntp spu spuctl sshd ssk stat stress su tar tc tcpdump telnetd touch tr64c tr69c true udhcpd udpechod umount uname uploadd upnp urlfilterd usb_modeswitch usleep vi vlanctl vsftpd watch wl wl_server wl_server_socket wlctl wlevt wlmngr wps_monitor wput xdslctl xtm xtmctl zebra zyxeldiagd
```

## /sbin
```
> echo $(ls /sbin)
3g-log.sh 3g.sh arp blkid depmod devmem ethctl fdisk freeramdisk fsck fsck.minix getty halt hotplug hotplug-usb.sh hwclock ifconfig ifdown ifup init insmod klogd logread losetup lsmod makedevs mdev mkfs.minix mkswap modprobe pivot_root poweroff reboot rmmod route runlevel sulogin swapoff swapon switch_root sysctl sysinfo syslogd udhcpc usb-find-devices.sh usb-get-ids.sh usb-get-name.sh usb-modeswitch.sh vconfig watchdog
```

## /usr/bin
```
brctl chpasswd dnsd fakeidentd flash_eraseall httpd inetd lpd telnetd udhcpd
 > echo $(ls /usr/bin)
[ [[ awk basename chat chrt chvt clear cmp cut diff du eject env ether-wake exfpr find fold free ftpget ftpput fuser head hexdump install iostat ipcrm ipcs killall killall5 less logger lpq lpr mesg mkfifo mpstat nc nslookup passwd pidstat printf pscan reset sadf sar sendarp seq setsid sort strings tac tail tcpsvd tee telnet test tftp tftpd time top traceroute traceroute6 tty unexpand uniq uptime wc wget which whoami xargs yes
```

## /usr/sbin
```
 > echo $(ls /usr/sbin)
brctl chpasswd dnsd fakeidentd flash_eraseall httpd inetd lpd telnetd udhcpd
```

```
 > echo $(ls /usr/local/jamvm/bin)
ls: /usr/local/jamvm/bin: No such file or directory
```

[Main Page](README.md)